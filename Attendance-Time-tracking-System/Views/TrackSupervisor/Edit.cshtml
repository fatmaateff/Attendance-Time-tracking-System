@model TrackSupervisor
@{
    Layout = "_MainView";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>
    <form method="post" class="m-5">

        <div class="form-group">
            <label asp-for="Track" class="control-label"></label>
            <select asp-for="Track" class="form-control">
                <option value="">Select Track</option>
                @foreach(var track in ViewBag.tracks)
                {
                    <option value="@track.id" >@track.name</option>
                }
            </select>
            <span asp-validation-for="Track" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Track" class="control-label"></label>
            <select asp-for="Track" class="form-control">
                <option value="">Select Track</option>
                @foreach (var track in ViewBag.tracks)
                {
                    <option value="@track.id">@track.name</option>
                }
            </select>
            <span asp-validation-for="Track" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Track" class="control-label"></label>
            <select asp-for="Track" class="form-control">
                <option value="">Select Track</option>
                @foreach (var track in ViewBag.tracks)
                {
                    <option value="@track.id">@track.name</option>
                }
            </select>
            <span asp-validation-for="Track" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Track" class="control-label"></label>
            <select asp-for="Track" class="form-control">
                <option value="">Select Track</option>
                @foreach (var track in ViewBag.tracks)
                {
                    <option value="@track.id">@track.name</option>
                }
            </select>
            <span asp-validation-for="Track" class="text-danger"></span>
        </div>


        <br />
        <div class="form-group">
            <button type="submit" class="btn btn-danger">
                <img width="30" height="30" src="~/images/upcr.png" alt="Update" /> Update
            </button>
        </div>
        <br />

    </form>
</body>
</html>