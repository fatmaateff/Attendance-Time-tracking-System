@model List<Permission>
@{
    Layout = "_MainView";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style>

        <style >
        /* Center the table horizontally */
        .center {
            display: flex;
            justify-content: center;
            align-items: center; /* Vertically center content */
            height: 100vh; /* Ensure full viewport height */
            margin: 0; /* Remove default margin */
        }

        /* Style the table header */
        th {
            background-color: #c62f36;
            color: white;
            padding: 10px;
        }

        /* Style the table */
        table {
            border-collapse: collapse;
            width: 100%;
        }

        /* Style table cells */
        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        /* Alternate row color */
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        .center-container {
            display: flex;
            justify-content: center;
            align-items: center; /* Vertically center content */
            margin-top: 20px; /* Add margin to the top */
            margin-bottom: 10px; /* Add margin to the bottom */
        }

        #searchInput {
            margin-right: 10px; /* Add some distance between input and search button */
        }
    </style>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Student Id</th>
                <th>Date</th>
                <th>Type</th>
                <th>Reason</th>
                <th>Status</th>
                <th>Accept</th>
                <th>Refuse</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var i in Model)
            {
                <tr>
                    <td>@i.StdId</td>
                    <td>@i.Date</td>
                    <td>@i.Type</td>
                    <td>@i.Reason</td>
                    <td>@i.Status</td>
                    <td><a class=" btn btn-success" asp-action="acceptpermisssion" asp-controller="SupervisorPermission" asp-route-date=@i.Date>Accept</a></td>
                    <td><a class="btn btn-danger" asp-action="refuse" asp-controller="SupervisorPermission" asp-route-date=@i.Date>Refuse</a></td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
